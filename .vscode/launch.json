{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/app",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make Build"
        }
    ]
}
